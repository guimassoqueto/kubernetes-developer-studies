apiVersion: v1
kind: Pod
metadata:
  name: sample
  labels:
    name: sample
spec:
  containers:
  - name: container-1
    image: sample-image-1
    resources:
      limits:
        memory: "128Mi"
        cpu: "500m"
  - name: container-2
    image: sample-image-2
    resources:
      limits:
        memory: "128Mi"
        cpu: "500m"